# Dependencies
node_modules/
server/node_modules/
client/node_modules/

# Build outputs
dist/
server/dist/
client/dist/

# Uploaded files
server/uploads/*
!server/uploads/.gitkeep

# Environment & SQLite DB
*.sqlite
*.sqlite-journal
.env
.env.local

# Google service account key (nunca deve ser versionada)
server/google-service-account.json

# Pasta de entrada temporária para import em lote (PDFs reais de professores/alunos —
# nunca deve ir para um repositório público; ver server/src/scripts/importOrientadorFiles.ts)
pbl1prof/

# Estado local de sessão do Claude Code (mantém launch.json, que é config de dev compartilhável)
.claude/*.lock

# OS & IDE
.vscode/
.idea/
.DS_Store
Thumbs.db
